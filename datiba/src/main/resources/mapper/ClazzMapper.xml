<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.datiba.mapper.ClazzMapper">

    <select id="listClass" resultType="com.datiba.domain.dto.MyClazzDto">
        select class.id as clazzId,user_class_record.id as recordId,class.name,class.code
        from class,user_class_record
        where class_id = class.id
          and user_class_record.user_id = #{userId}
    </select>
</mapper>

